{
  "name": "zumbi-bot",
  "private": true,
  "version": "1.0.0",
  "description": "Zumbi Bot - A revolução dos robôs no apocalipse zumbi. Beat 'em up 2.5D no navegador.",
  "type": "module",
  "license": "CC0-1.0",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --port 4173 --strictPort",
    "build:app": "tsc --noEmit && vite build --mode app",
    "android:sync": "npm run build:app && cap sync android",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "e2e": "playwright test",
    "format": "prettier --write .",
    "check": "npm run typecheck && npm run test && vite build"
  },
  "dependencies": {
    "n8ao": "2.0.1",
    "peerjs": "1.5.5",
    "postprocessing": "6.39.5",
    "qrcode-generator": "2.0.4",
    "three": "0.186.1"
  },
  "devDependencies": {
    "@capacitor/android": "8.5.2",
    "@capacitor/cli": "8.5.2",
    "@capacitor/core": "8.5.2",
    "@playwright/test": "1.56.1",
    "@types/node": "^22.20.4",
    "@types/three": "0.186.0",
    "peer": "1.0.2",
    "prettier": "^3.9.9",
    "typescript": "6.0.3",
    "vite": "^8.3.1",
    "vitest": "^5.0.2"
  }
}
